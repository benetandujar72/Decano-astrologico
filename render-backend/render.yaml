services:
  - type: web
    name: fraktal-api
    runtime: node
    region: frankfurt  # Más cercano a España
    plan: free  # Cambia a 'starter' para producción
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GOOGLE_API_KEY
        sync: false  # Configurar manualmente en dashboard
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: https://myfraktal.com,https://asgnyckayusnmbozocxh.supabase.co
    healthCheckPath: /health
    autoDeploy: true  # Deploy automático al hacer push a main
