# Frontend (Vite)
# URL del backend (Render/FastAPI). No incluir '/' al final.
VITE_API_URL=http://localhost:8000

# Nota: NO pongas secretos del backend aquí.
# El frontend no debería necesitar GEMINI_API_KEY en producción.
