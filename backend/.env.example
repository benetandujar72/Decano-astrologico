# ============================================
# FRAKTAL API - Variables de Entorno
# ============================================
# IMPORTANTE: Copia este archivo a .env y completa con tus valores reales
# NO subas el archivo .env a GitHub (ya está en .gitignore)

# ============================================
# MongoDB Configuration
# ============================================
# Para MongoDB Atlas (RECOMENDADO para producción):
# 1. Crea cuenta gratuita en https://www.mongodb.com/cloud/atlas
# 2. Crea un cluster (tier gratuito M0)
# 3. En "Database Access": crea un usuario con contraseña
# 4. En "Network Access": permite acceso desde 0.0.0.0/0 (todas las IPs)
# 5. Obtén la connection string desde "Connect" > "Connect your application"
# 6. Reemplaza <password> con tu contraseña real
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/fraktal?retryWrites=true&w=majority

# Ejemplo local (solo desarrollo):
# MONGODB_URI=mongodb://localhost:27017

# ============================================
# Security (JWT)
# ============================================
# Genera una clave aleatoria segura (256 bits):
# Python: python -c "import secrets; print(secrets.token_urlsafe(32))"
# O usa: https://generate-secret.vercel.app/32
SECRET_KEY=REEMPLAZAR_CON_CLAVE_ALEATORIA_SEGURA_DE_256_BITS

# ============================================
# Admin Bootstrap
# ============================================
# Contraseña inicial del usuario admin@programafraktal.com
# ⚠️ CAMBIAR en producción por seguridad
ADMIN_BOOTSTRAP_PASSWORD=1234

# ============================================
# CORS (Frontend URLs permitidas)
# ============================================
# Agrega aquí las URLs de tu frontend en Vercel
# Separa múltiples URLs con comas
CORS_ORIGINS=https://tu-app.vercel.app,https://decano-astrologico.vercel.app

# ============================================
# API Keys (Opcional)
# ============================================
# API Key de Google Gemini (si usas generación de contenido)
GEMINI_API_KEY=

# ============================================
# INSTRUCCIONES PARA RENDER
# ============================================
# En Render Dashboard > Environment:
# 1. Agrega cada variable de arriba (sin el prefijo VITE_)
# 2. Marca como "Secret" las sensibles (SECRET_KEY, MONGODB_URI)
# 3. MONGODB_URI debe apuntar a MongoDB Atlas
# 4. CORS_ORIGINS debe incluir tu URL de Vercel
#
# Ejemplo:
# MONGODB_URI = mongodb+srv://user:pass@cluster.mongodb.net/fraktal
# SECRET_KEY = abc123xyz789...
# ADMIN_BOOTSTRAP_PASSWORD = tu_password_seguro
# CORS_ORIGINS = https://tu-app.vercel.app
